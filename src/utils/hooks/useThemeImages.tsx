export const useThemeImages = () => {
  // const scheme = useColorScheme();
  // const themeImages: ThemeType["images"] =
  //   scheme === "light" ? lightTheme.images : darkTheme.images;
  // return themeImages;
};
